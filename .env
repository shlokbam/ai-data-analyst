# .env  —  Environment Variables (SECRET FILE — never commit to Git!)
# -----------------------------------------------------------------------
# This file holds sensitive values that should NOT be in your code.
# python-dotenv reads this file when your app starts (via load_dotenv()).
#
# HOW TO USE:
#   Replace the placeholder values below with your actual keys.
#   Your Python code reads them with: os.getenv('VARIABLE_NAME')

# Your Gemini API key from aistudio.google.com
GEMINI_API_KEY=AIzaSyDX3l015mE-bSJp5gTyPm45bDRED9wHoLY

# A long random string used to encrypt session cookies.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace_this_with_a_long_random_string

# Upload folder path (leave as-is for local dev)
UPLOAD_FOLDER=uploads